#include "main.h"

/**
 * mul - multiplies towo integers.
 * @a: is the first factor to mutiplie,
 * @b: is the second factor to multiplie,
 * Return: zero.
*/

int mul(int a, int b)
{
	return (a * b);
}
