#include "main.h"
#include <stdio.h>

/**
 * _islower - check each integer passed
 * and return 1 if lowercase
 * or 0 if not,
 * with a new line
 * Return: zero
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
